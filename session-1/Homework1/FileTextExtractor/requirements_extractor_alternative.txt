# Enhanced File Text Extractor Dependencies - Windows-friendly
# Core libraries
SpeechRecognition==3.10.0
pydub==0.25.1
PyPDF2==3.0.1
# Replace PyMuPDF with pdfplumber (more Windows-friendly)
pdfplumber==0.9.0
opencv-python==4.8.1.78
pytesseract==0.3.10
Pillow==10.0.1

# Additional dependencies
pyaudio==0.2.11
numpy<2

# Alternative PDF processing libraries
# pdf2image==1.16.3
# PyPDF2 already included above

# Note: For video processing, the enhanced version now:
# 1. Extracts audio from videos and uses speech recognition (primary method)
# 2. Falls back to OCR frame analysis if audio extraction fails
# 3. Automatically handles long audio/video files by splitting into segments
# 4. Provides better error handling and user feedback
